# block-example-simple-1

The absolute bare minimum a block needs to exist in DGBlocks.

- Depends only on `block-core`
- No loggers, no RTC members, no hooks, no PropertyGroups, no operators, no panels
- Registers an empty block instance so the framework knows it exists
